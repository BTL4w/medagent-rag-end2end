# medagent-rag-end2end

## Quickstart

1. Copy env: `copy .env.example .env` (Windows) và điền biến cần thiết.
2. **Docker:** `docker compose up --build` → mở http://localhost:8000/docs và http://localhost:8000/chat-ui/
3. **Python:** xem chi tiết trong [deploy/RUNBOOK.md](deploy/RUNBOOK.md).
